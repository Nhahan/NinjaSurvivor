using UnityEngine;

namespace AdSkills
{
    public class DiagonalStar : AdSkill
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
